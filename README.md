# Synnical SVG

This repository publishes the SVG launcher for the live Synnical OS deployment.

The current `index.svg` is intentionally a lightweight launcher that points at `https://synnical.co.uk` instead of shipping stale app code inside the SVG bundle. Use a commit-pinned jsDelivr URL for stable embeds.
